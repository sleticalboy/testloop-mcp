import { describe, it, expect } from 'vitest';
import { formatText } from './js_esm_branch';

describe('formatText', () => {
  it('covers vitest formatText short branch', () => {
    // coverage task: vitest-esm-task-1 | lines 2-4 | 断言 Vitest ESM 分支返回值 | 构造满足条件 `mode === 'short'` 的输入
    const result = formatText('short', 'test', 'test');
    expect(result).toBe(('test'));
  });

});
